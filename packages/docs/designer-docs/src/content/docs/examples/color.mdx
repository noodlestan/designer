---
title: Color Examples
sidebar:
    title: Color
description: Examples of color decisions documented with designer components.
---

import { CardGrid } from '@astrojs/starlight/components';
import { ShowDecision, ShowDecisionCard } from '@noodlestan/designer-shows/astro';

import { decisionLoader } from '../../../decisions';

export const store = await decisionLoader();

```mdx
<ShowDecisionCard store={store} d="Action Color" value="hsl" model params>
```

<div>
    <ShowDecisionCard store={store} d="Action Color" value="hsl" model params />
</div>

```mdx
<ShowDecision store={store} d="Brand Pink Hue" />
<ShowDecision store={store} d="Brand Blue Hue" />
```

<CardGrid>
    <ShowDecision store={store} d="Brand Pink Hue" size="l" />
    <ShowDecision store={store} d="Brand Blue Hue" size="l" />
</CardGrid>

```mdx
<ShowDecision store={store} d="Curcuma Palette" />
```

<CardGrid>
    <ShowDecision store={store} d="Curcuma Palette" />
</CardGrid>

```mdx
<ShowDecision store={store} d="Action Color" size="s" value={['rgb', 'hsl']} />
```

<CardGrid>
    <ShowDecision store={store} d="Action Color" size="s" value={['rgb', 'hsl']} />
</CardGrid>

```mdx
<ShowDecision store={store} d="Foreground Lightness Scale" size="m" />
<ShowDecision store={store} d="Background Lightness Scale" size="m" />
```

<CardGrid>
    <ShowDecision store={store} d="Foreground Lightness Scale" size="m" />
    <ShowDecision store={store} d="Background Lightness Scale" size="m" />
</CardGrid>
