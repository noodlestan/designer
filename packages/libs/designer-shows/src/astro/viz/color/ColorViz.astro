---
import type { ColorVizProps } from '../../types';

type Props = ColorVizProps;

const { value, size = 'auto' } = Astro.props;

const hsl = value?.getString('hsl');
---

<div class:list={['dd-color-value-viz', size]}><div class="swatch"></div></div>

<style define:vars={{ color: hsl }}>
    .dd-color-value-viz {
        width: 100%;
        min-width: var(--dd-value-min-width);
    }

    .xs {
        min-width: var(--dd-value-size-xs);
        max-width: var(--dd-value-size-xs);
    }

    .s {
        min-width: var(--dd-value-size-s);
        max-width: var(--dd-value-size-s);
    }

    .m {
        min-width: var(--dd-value-size-m);
        max-width: var(--dd-value-size-m);
    }

    .l {
        min-width: var(--dd-value-size-l);
        max-width: var(--dd-value-size-l);
    }

    .xl {
        min-width: var(--dd-value-size-xl);
        max-width: var(--dd-value-size-xl);
    }

    .swatch {
        width: 100%;
        min-height: var(--dd-value-min-width);
        aspect-ratio: 1 / 1;
        background-color: var(--color);
        border: var(--dd-color-viz-border);
        border-radius: var(--dd-color-viz-border-radius);
    }
</style>
