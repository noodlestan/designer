// import type { BaseValue } from '../../values/base/base-value';

export type NumberFormatOptions = {
    quantize?: number;
};

// export type BaseNumericValue<T extends number = number> = BaseValue<T> & {
//     quantized: (quantize?: number) => T;
// };

// export type AbstractNumericValue = Omit<BaseNumericValue, 'type' | 'context'>;
