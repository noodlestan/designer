---
title: createStaticStore()
slug: api/designer-functions/Store/createStaticStore
sidebar:
    label: createStaticStore
---

```ts
type createStaticStore = (
    inputStore: StaticInputMap, //
) => StaticStore;
```

## Return value

Returns a [StaticStore](/api/designer-functions/Store/Types/StaticStore) that can be used to retrieve decision and resolve decision values.

## Params

#### ༶ `inputStore`: [StaticInputMap](/api/designer-decisions/Inputs/Types/StaticInputMap)

&nbsp;

## Usage

```ts
const inputMap = createStaticInputMap(inputData, validator);
const store = createStaticStore(inputMap);
```
