---
title: produceDecisions()
slug: api/designer-functions/cli-helpers/produceDecisions
sidebar:
    label: produceDecisions
---

```ts
type produceDecisions = (store: Store) => ProducedDecisionStore;
```

Given a populated [Store](/api/designer-functions/Store/Types/Store) produces all possible decision values and exposes all decision values, contexts and aggregated errors.

## Return Value

Returns a `ProducedDecisionStore` object.

```ts
export type ProducedDecisionStore = {
    decisions: () => ProducedDecisionStatus[];
    hasErrors: () => boolean;
    errors: {
        count: () => number;
        store: () => StaticStoreError[];
        validation: () => InputValidationError[];
        value: () => (DecisionError | DecisionValueError)[];
    };
    summary: () => string;
};
```

## Params

#### ༶ `store`: [Store](/api/designer-functions/Store/Types/Store)

&nbsp;

## Usage

```js
const store = await build();
const produced = produceDecisions(store);
console.info(produced.summary());
```

## See also

-   [Guides / Loading and Validating](/guides/loading-and-validating-design-decisions/)
-   [API / designer-decisions / produceDecisionStatus()](/api/designer-decisions/cli-helpers/produceDecisionStatus)
-   [API / designer-decisions / formatDecisionStatus()](/api/designer-decisions/cli-helpers/formatDecisionStatus)
