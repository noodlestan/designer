---
title: produceDecisions()
slug: api/designer-functions/cli-helpers/produceDecisions
sidebar:
    label: produceDecisions
---

```ts
type produceDecisions = (context: BuilderContext, store: Store) => ProducedDecisionStore;
```

Given a populated [Store](/api/designer-decisions/Store/Types/Store) produces all possible decision values and exposes all decision values, contexts and aggregated errors.

## Return Value

Returns a `ProducedDecisionStore` object.

```ts
export type ProducedDecisionStore = {
    decisions: () => ProducedDecisionStatus[];
    hasErrors: () => boolean;
    summary: () => string;
};
```

## Params

#### ༶ `context`: [Store](/api/designer-functions/Builder/Types/BuilderContext)

## Params

#### ༶ `store`: [Store](/api/designer-decisions/Store/Types/Store)

&nbsp;

## Usage

```js "produceDecisions" "decisions()" "summary()" "hasErrors()"
const context = createBuilderContext(options);
const build = buildStaticStore(context);
const store = await build();

const produced = produceDecisions(context, store);
const decisionStatuses = produced.decisions();
const summary = produced.summary();

decisionStatuses.forEach(status => console.info(formatDecisionStatus(status)));
console.info(summary);

if (produced.hasErrors()) {
    throw new Error(`Errors encountered producing decisions.`);
}
```

## See also

-   [Guides / Loading and Validating](/guides/loading-and-validating-design-decisions/)
-   [API / designer-decisions / produceDecisionStatus()](/api/designer-decisions/cli-helpers/produceDecisionStatus)
-   [API / designer-decisions / formatDecisionStatus()](/api/designer-decisions/cli-helpers/formatDecisionStatus)
