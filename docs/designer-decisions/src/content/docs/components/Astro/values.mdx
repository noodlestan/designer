---
title: Value Components
slug: components/Astro/Values
description: Astro components that render design decision viz and value together.
sidebar:
    order: 20
    label: Decision Values
---

import { CardGrid } from '@astrojs/starlight/components';
import { ShowColorValue, ShowSpaceValue } from '@noodlestan/designer-shows/astro';

import { decisionLoader } from '../../../../decisions';
import {
    createColorValue,
    createDecisionContext,
    createDecisionValueContext,
    createSpaceValue,
} from '@noodlestan/designer-decisions';

export const store = await decisionLoader();

export const context = store.createDecisionContext();
export const colorValue = createColorValue(context, '#ff0000');
export const spaceValue = createSpaceValue(context, 12);

These components render a decision's visualization and value directly.

Tip: 🚧 You don't have to use these components directly in your docs.
Using [ShowDecision](/components/Astro/Decisions/show-decision/) will select
the appropriate one for you.

They are exposed so that anyone can
[create custom visualizations](/guides/creating-custom-design-decision-visualizations/),
composing values and visualizations directly in their custom components.

In the [ShowDecision](/components/Astro/Decisions/show-decision) reference,
you will find all decision types and their corresponding value components.

## Common Props

All of these components share the same [common props](/components/Astro/Props/#valueprops) –
`value`, `size`, `viz`, and `options` – to control how the visualization
and value are displayed.

Decision components, such as `<ShowDecision/>` expose these props as well, so that you
can have full control of the options.

🚧 Note: props are not strongly typed there, as they depend on decision type.

## Color

Color related components used by
[ShowDecision (color decisions)](/components/Astro/Decisions/show-decision/#color).

### ShowColorValue

##### ༶ `viz`: [ColorVizName](/api/designer-shows/Types/ColorVizName)

##### ༶ `options`: [ColorVizOptions](/api/designer-shows/Types/ColorVizOptions)

##### Example

```mdx
export const colorValue = createColorValue(context, '#ff0000');

<ShowColorValue size="s" v={colorValue} />
```

<CardGrid>
    <ShowColorValue size="s" v={colorValue} />
</CardGrid>

### ShowColorChannel

🚧 https://github.com/noodlestan/designer/issues/14

## Space

Space related components used by
[ShowDecision (space decisions)](/components/Astro/Decisions/show-decision/#space).

### ShowSpaceValue

##### ༶ `viz`: [SpaceVizName](/api/designer-shows/Types/SpaceVizName/)

##### Example

```mdx
export const spaceValue = createSpaceValue(context, 12);

<ShowSpaceValue size="s" v={spaceValue} />
```

<CardGrid>
    <ShowSpaceValue size="s" v={spaceValue} />
</CardGrid>
