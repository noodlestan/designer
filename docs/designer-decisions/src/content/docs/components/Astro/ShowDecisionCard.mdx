---
title: 'ShowDecisionCard'
slug: components/Astro/ShowDecisionCard
description: Astro component to visualize any decision along with its details in a card layout.
sidebar:
    order: 20
---

import { Aside } from '@astrojs/starlight/components';
import { ShowDecision, ShowDecisionCard } from '@noodlestan/designer-shows/astro';

import { decisionLoader } from '../../../../decisions';

export const store = await decisionLoader();

```ts
import { ShowDecisionCard } from '@noodlestan/designer-shows/astro';
```

Renders a card layout with the appropriate visualization according to the decision type, along with any decision details you might want to display.

By default it renders only the decision **description** and **usage** guidelines.

<ShowDecisionCard store={store} d="Action Color" />

## Composes

This component composes:

-   [ShowDecision](/components/Astro/ShowDecision) component (value and visualization)
-   inside of `DecisionCardLayout` (🚧 link)
-   and one or more [Decision Attribute](/components/Astro/Composable/Attributes).

## Props

### ༶ [DecisionProps](/components/Astro/Props): `store`, `d` and `layout`

These two **mandatory** `store` and `d` props serve to identify and retrieve the decision.

The optional `layout` prop controls the flow of multiple items and the value/viz pair.

### ༶ [ValueProps](/components/Astro/Props#valueprops): `value`

This prop control whether the value should be shown and how to format it.

Refer to the table on [Composable Types](/components/Astro/Composable/Types) to learn which `value` options are available for each decision type.

### ༶ [VizProps](/components/Astro/Props#vizprops): `viz`, `size`, and `options`

These props control whether the visualization should be shown, which specific visualization to show, and how to customize it.

In this example we are rendering a [ColorValueDecision](/decision-models/color-value), hiding its value and increasing the size of the color swatch.

```mdx
<ShowDecisionCard store={store} d="Action Color" value={false} size="l" />
```

<ShowDecisionCard store={store} d="Action Color" value={false} size="l" />

Refer to the table on [Composable Types](/components/Astro/Composable/Types) for details on available visualizations and options.

### ༶ `name` (true): boolean

Provide this flag to hide the decision's name.

```mdx
<ShowDecisionCard store={store} d="Space Unit" name={false} />
```

<ShowDecisionCard store={store} d="Space Unit" name={false} />

### ༶ `description` (true): boolean

Provide this flag to hide the decision's description.

```mdx
<ShowDecisionCard store={store} d="Action Color" description={false} />
```

<ShowDecisionCard store={store} d="Action Color" description={false} />

### ༶ `usage` (true): boolean

Provide this flag to hide the decision usage guidelines.

```mdx
<ShowDecisionCard store={store} d="Action Color" usage={false} />
```

### ༶ `model` (optional): boolean

### ༶ `params` (optional): boolean

Provide these flag to display the decision model and input params at the bottom of the card

```mdx
<ShowDecisionCard store={store} d="Action Color" model params />
```

<ShowDecisionCard store={store} d="Action Color" model params />

### ༶ `contexts` (optional): boolean

Provide this flag to display the decision's contexts at the bottom of the card

```mdx
<ShowDecisionCard store={store} d="Action Color" contexts />
```

<ShowDecisionCard store={store} d="Action Color" contexts />

### ༶ `data` (optional): boolean

Provide this flag to display the decision's raw data at the bottom of the card

```mdx
<ShowDecisionCard store={store} d="Action Color" data />
```

<ShowDecisionCard store={store} d="Action Color" data />
