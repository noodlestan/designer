---
title: Attribute Components
slug: components/Astro/Attributes
description: Astro components that render design decision details and metadata.
sidebar:
    order: 30
    label: Decision Attributes
---

import { CardGrid } from '@astrojs/starlight/components';
import {
    FlexStack,
    ShowDecisionContexts,
    ShowDecisionData,
    ShowDecisionDescription,
    ShowDecisionModel,
    ShowDecisionParams,
    ShowDecisionUsage,
} from '@noodlestan/designer-shows/astro';

import { decisionLoader } from '../../../../decisions';

export const store = await decisionLoader();

These components render decision details and metadata.

Tip: 🚧 You don't have to use these components directly in your docs.
Using [ShowDecision](/components/Astro/Decisions/show-decision/) will select
the appropriate one for you.

They are exposed so that anyone can
[create custom visualizations](/guides/creating-custom-design-decision-visualizations/),
composing these directly in their custom components.

## Common Props

All of these components share the same [common props](/components/Astro/Props/#decisionprops) –
`d` and `store` – to identify and retrieve the decision.

## ShowDecisionModel

Renders the [model](/decision-models) used in the decision.

```mdx
<ShowDecisionModel store={store} d="Pink Palette" />
```

<FlexStack vpad>
    <ShowDecisionModel store={store} d="Pink Palette" />
</FlexStack>
, , ## ShowDecisionDescription

Renders the decision's description.

```mdx
<ShowDecisionDescription store={store} d="Pink Palette" />
```

<FlexStack vpad>
    <ShowDecisionDescription store={store} d="Pink Palette" />
</FlexStack>

## ShowDecisionUsage

Renders the decision's usage guidelines.

```mdx
<ShowDecisionUsage store={store} d="Pink Palette" />
```

<FlexStack vpad>
    <ShowDecisionUsage store={store} d="Pink Palette" />
</FlexStack>

## ShowDecisionParams

Renders the decision parameters.

```mdx
<ShowDecisionParams store={store} d="Pink Palette" />
```

<FlexStack vpad>
    <ShowDecisionParams store={store} d="Pink Palette" />
</FlexStack>

## ShowDecisionContexts

Renders the decision contexts.

```mdx
<ShowDecisionContexts store={store} d="Pink Palette" />
```

<FlexStack vpad>
    <ShowDecisionContexts store={store} d="Pink Palette" />
</FlexStack>

## ShowDecisionData

Renders the decision raw data.

```mdx
<ShowDecisionData store={store} d="Pink Palette" />
```

<FlexStack vpad>
    <ShowDecisionData store={store} d="Pink Palette" />
</FlexStack>
