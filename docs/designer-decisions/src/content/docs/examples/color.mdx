---
title: Color Examples
description: Examples of color decisions documented with designer components.
sidebar:
    title: Color
---

import { CardGrid } from '@astrojs/starlight/components';
import { FlexRow, ShowDecision, ShowDecisionCard } from '@noodlestan/designer-shows/astro';

import { decisionLoader } from '../../../decisions';

export const store = await decisionLoader();

```mdx
<ShowDecisionCard store={store} d="Brand Pink" value={['hsl', 'rgb', 'oklch']} model params />
```

<div>
    <ShowDecisionCard store={store} d="Brand Pink" value={['hsl', 'rgb', 'oklch']} model params />
</div>

```mdx
<ShowDecision store={store} d="Brand Pink Hue" size="l" />
<ShowDecision store={store} d="Brand Blue Hue" size="l" />
```

<CardGrid>
    <ShowDecision store={store} d="Brand Pink Hue" size="l" />
    <ShowDecision store={store} d="Brand Blue Hue" size="l" />
</CardGrid>

```mdx
<ShowDecision store={store} d="Brand Color Set" size="m" row />
```

<FlexRow vpad>
    <ShowDecision
        store={store}
        d="Brand Color Set"
        size="m"
        layout="row"
        viz="fg"
        options={{ contrast: '#ccc' }}
    />
</FlexRow>

```mdx
<ShowDecision store={store} d="Pink Palette" />
<ShowDecision store={store} d="Heat Map Color Set" />
```

<CardGrid>
    <ShowDecision store={store} d="Pink Palette" />
    <ShowDecision store={store} d="Heat Map Color Set" value={['hsl', 'oklch']} />
</CardGrid>

```mdx
<ShowDecision store={store} d="Action Color" size="s" value={['rgb', 'oklch']} />
```

<CardGrid>
    <ShowDecision
        store={store}
        d="Action Color"
        size="s"
        viz="fg"
        options={{ contrast: 'oklch(24% 0.08 257)' }}
        value={['rgb', 'oklch']}
    />
</CardGrid>

```mdx
<ShowDecision
    store={store}
    d="Foreground Lightness Scale"
    size="m"
    viz="fg"
    options={{ contrast: 'oklch(24% 0.08 257)' }}
/>
<ShowDecision
    store={store}
    d="Background Lightness Scale"
    size="m"
    viz="fg"
    options={{ contrast: 'oklch(24% 0.08 257)' }}
/>
```

<CardGrid>
    <ShowDecision
        store={store}
        d="Foreground Lightness Scale"
        size="m"
        viz="fg"
        options={{ contrast: 'oklch(24% 0.08 257)' }}
    />
    <ShowDecision
        store={store}
        d="Background Lightness Scale"
        size="m"
        viz="fg"
        options={{ contrast: 'oklch(24% 0.08 257)' }}
    />
</CardGrid>
